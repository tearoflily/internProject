<%= link_to '戻る', employee_item_path(@item), class: 'btn btn-secondary btn-lg ml-3 mr-1 mb-3' %>
<%= link_to '前月', employee_product_sales_amount_path(@item, params:{month: @month.prev_month}), class: 'btn btn-primary btn-lg ml-3 mb-3' %>
<% if @month < Date.today.beginning_of_month %>
  <%= link_to '今月へ', employee_product_sales_amount_path(@item), class: 'btn btn-primary btn-lg ml-3 mb-3' %>
<% end %>

<% if @graphDatas.count > 0 %>
 <%= bar_chart @graphDatas  %>
<% else %>
<% end %>