<% if orders.count >0 %>  
  <table class="table table-bordered">
    <thead>
      <tr>
        <th class="bg-dark text-white font-weight-bold text-center">お客様名</th>
        <th class="bg-dark text-white font-weight-bold text-center">注文件数</th>
        <th class="bg-dark text-white font-weight-bold text-center">買い上げ点数</th>
        <th class="bg-dark text-white font-weight-bold text-center"></th>
      </tr>
    </thead>  
    <tbody>
    <% orders.each do |order| %>
      <tr>
        <td><%= image_tag(employee_item_image_path(item.id),size:'80') %></td>
        <td class="font-weight-bold text-center w-25 align-middle"></td>
        <td class="font-weight-bold text-center align-middle"></td>
        <td class="text-center align-middle">
          <%= link_to '編集', "#", class: 'btn btn-primary btn-lg mr-3' %>
        </td>
      </tr>
    <% end %>
    </tbody>
  </table>
 <% else %> 
   <div class="p-5 bg-secondary text-center text-white font-weight-bold">まだ注文がありません。</div>
 <% end %>