<nav class="navbar navbar-light navbar-expand-md bg-dark fixed-top">
  <div class="customer_header">
    <%= link_to "お魚屋さん", top_show_path, id: "logo" %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#responsiveMenu" aria-controls="responsiveMenu" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="responsiveMenu">
      <% if logged_in? %>
        <ul class="navbar-nav">
          <li class="nav-item"><%= link_to "お買い物かご", "#" %></li>
          <li class="nav-item"><%= link_to "マイページ", customer_user_path(current_user) %></li>
          <li class="nav-item"><%= link_to "ログアウト", logout_path, method: :delete %></li>
        </ul>
      <% else %>
        <ul class="navbar-nav">
          <li class="nav-item"><%= link_to "ログイン", login_url %></li>
        </ul>
      <% end %>
    </div>
  </div>
</nav>
 <% flash.each do |message_type, message| %>
   <div class="alert alert-<%= message_type %>"><%= message %></div>
 <% end %>