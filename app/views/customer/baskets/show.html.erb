<%= provide(:title, '注文一覧') %>

<div class="container">
  <div class="row">
    <div class="col-md-12">

        <!-- pcの場合-->
        <div class="baskets-pc">
        <%= render 'customer/baskets/PC/show', { user: @user } %>
        </div>

        <!-- pcの場合-->
        <div class="baskets-phone">
        <%= render 'customer/baskets/Phone/show', { user: @user } %>
        </div>
    </div>
  </div>
</div>

